export type PostCardPost = {
    userFullName: string | null,
    userName: string | null,
    avatarUrl: string | null,
    content: string,
    likes: any[] | null,
    userId: string | null,
    postId: string | null
}